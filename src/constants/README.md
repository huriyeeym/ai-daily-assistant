# Constants

Application constants and enums.

## Files

- `colors.ts` - Color constants
- `emotions.ts` - Emotion types and mappings
- `storage.ts` - AsyncStorage keys
- `api.ts` - API related constants
